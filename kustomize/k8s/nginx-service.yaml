apiVersion: v1
kind: Service
metadata: 
  name: hello-world
  labels:
    app: teamtech
spec:
  type: LoadBalancer
  ports:
  - name: teamtech
    port: 80 
    protocol: TCP
    targetPort: 80
  - name: nginx-prometheus-exporter
    port: 9113
    targetPort: 9113
  selector:
    app: teamtech
