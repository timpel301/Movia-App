apiVersion: v1
kind: Service
metadata: 
  name: hello-world
  labels:
    app: teamtech
spec:
  type: LoadBalancer
  ports:
    - port: 80 
      protocol: TCP
      targetPort: 80
  selector:
    app: teamtech
