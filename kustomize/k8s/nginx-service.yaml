apiVersion: v1beta1
kind: Service
metadata: 
  name: Hello-world
  labels:
    app: teamtech
spec:
  type: ClusterIP
  ports:
    - port: 80 
      protocol: TCP
      targetPort: 80
  selector:
    app: teamtech
